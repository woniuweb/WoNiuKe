<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
		PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.naccl.mapper.UploadFileMapper">

	<resultMap id="UploadFile" type="top.naccl.entity.UploadFile">
		<id column="id" property="id"/>
		<result column="storage_mode" property="storageMode"/>
		<result column="file_type" property="fileType"/>
		<result column="original_name" property="originalName"/>
		<result column="file_name" property="fileName"/>
		<result column="file_path" property="filePath"/>
		<result column="url" property="url"/>
		<result column="file_size" property="fileSize"/>
		<result column="create_time" property="createTime"/>
	</resultMap>

	<insert id="save" parameterType="top.naccl.entity.UploadFile" useGeneratedKeys="true" keyProperty="id">
		insert into upload_file
		(storage_mode, file_type, original_name, file_name, file_path, url, file_size, create_time)
		values
		(#{storageMode}, #{fileType}, #{originalName}, #{fileName}, #{filePath}, #{url}, #{fileSize}, #{createTime})
	</insert>

	<select id="getById" resultMap="UploadFile">
		select id, storage_mode, file_type, original_name, file_name, file_path, url, file_size, create_time
		from upload_file
		where id = #{id}
	</select>

</mapper>
