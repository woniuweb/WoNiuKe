# NBlog 仓库协作规范

本仓库是一个多应用博客系统，包含 3 个主要子项目：

- `blog-api`：后端服务，技术栈为 Spring Boot 2.2、Maven、Java 8
- `blog-cms`：后台管理端，技术栈为 Vue 2 + Vue CLI
- `blog-view`：前台展示端，技术栈为 Vue 2 + Vue CLI

Agent 在本仓库内工作时，默认遵循以下规则。目标是：改动范围清晰、验证方式明确、尽量不打扰用户已有工作。

## 1. 先分清改动属于哪个子项目

- 后端逻辑、接口、数据访问层改动，只放在 `blog-api`
- 后台管理相关页面和接口调用改动，只放在 `blog-cms`
- 前台展示相关页面和接口调用改动，只放在 `blog-view`
- 除非需求本身跨前后端，否则不要把 3 个子项目混在一次改动里
- 优先做“小而完整”的改动，不做大范围顺手重构

## 2. 使用对应子项目自己的命令

- 后端相关操作在 `blog-api` 目录执行
- 前端相关操作分别在 `blog-cms` 或 `blog-view` 目录执行
- 优先执行与当前改动最相关的验证命令，不要默认跑全仓库重任务
- 不要随意删除 `target`、`dist`、缓存目录或 lockfile，除非用户明确要求

常用命令：

| 子项目 | 命令 | 用途 |
| --- | --- | --- |
| `blog-api` | `mvn test` | 运行后端测试 |
| `blog-api` | `mvn -DskipTests package` | 构建后端包 |
| `blog-cms` | `npm run serve` | 启动后台本地开发环境 |
| `blog-cms` | `npm run build` | 构建后台生产包 |
| `blog-view` | `npm run serve` | 启动前台本地开发环境 |
| `blog-view` | `npm run build` | 构建前台生产包 |

## 3. 默认验证要求

- 修改 `blog-api` 后，能跑时优先跑 `mvn test`
- 修改前端后，能跑时至少跑对应项目的 `npm run build`
- 如果改动涉及接口字段、请求参数、返回结构，要同时检查调用方和服务端是否一致
- 如果改动涉及配置文件，要说明影响的是哪个环境或部署面
- 如果因为环境限制无法验证，最终说明里必须明确写出“没有验证什么、为什么没验证”

## 4. 后端开发约束（`blog-api`）

- 遵守现有包结构：`controller`、`service`、`service.impl`、`mapper`、`entity`、`model`、`config`、`util`
- `controller` 保持薄，业务逻辑尽量放在 `service`
- 如果改了 `mapper` 方法，必须同步检查对应的 MyBatis XML 或持久层实现
- 优先复用现有 `DTO` / `VO` 设计，不要随意新增重复的数据结构
- 保持与 Java 8、Spring Boot 2.2 兼容
- 不要擅自引入新框架、大版本升级或明显超出现有架构的方案
- `application*.properties` 视为高风险文件，不要写死密钥、令牌、机器地址或临时配置

## 5. 前端开发约束（`blog-cms`、`blog-view`）

- 保持现有 Vue 2 + Vue CLI 架构
- 未明确要求时，不要引入 Vue 3、Vite、Composition API、TypeScript
- 按现有目录职责放代码：
  - `src/api`：接口调用
  - `src/store`：状态管理
  - `src/router`：路由
  - `src/components`：复用组件
  - `src/views`：页面
- 优先复用现有 Axios 封装和请求工具，不要重复造 HTTP client
- `blog-cms` 继续遵循 Element UI 的后台风格
- `blog-view` 保持现有前台组件、插件和页面组织方式
- 避免为了“顺手优化”造成大面积样式漂移或目录迁移

## 6. 配置和部署文件要谨慎

- `nginx.conf`、部署脚本、Spring profile 配置都属于高风险改动
- 修改这类文件时，要说明预期运行效果
- 能验证时尽量验证语法、构建结果或关联行为
- 未明确要求时，不要修改生产域名、端口、上传地址、账号密钥等关键配置

## 7. 尊重当前工作区已有改动

- 本仓库当前可能已经有用户未提交的修改
- 不要回滚与当前任务无关的本地改动
- 修改文件前，先看该文件是否已经有未提交变更
- 如果现有本地改动和当前任务直接冲突，要先明确指出冲突，而不是强行覆盖

## 8. 编码风格

- 优先保持与周边代码一致，不要为了“更现代”去硬改老代码风格
- 注释尽量短，只在代码意图不明显时补充
- 不做无意义的重命名、纯格式化、批量整理 import 等噪声改动
- 修 bug 时，优先选择“最小但完整”的修复方案，再配合验证

## 9. 最终交付说明要求

- 说明这次改了什么
- 说明改动落在哪个子项目
- 说明跑了哪些验证命令
- 说明哪些命令没跑、为什么没跑
- 如果改动跨前后端但只验证了一侧，要明确提示残余风险

## 10. 简单决策原则

- 不确定改哪里时，先判断属于 `blog-api`、`blog-cms` 还是 `blog-view`
- 不确定怎么改时，先参考附近已有实现
- 不确定要不要扩大改动时，默认不要扩大
- 不确定要不要交付时，先看是否完成了最基本验证

如果没有更强的任务要求，默认做法是：在正确的子项目里做最小正确改动，并用影响范围内最合适的命令完成验证。
